# This app supports dynamic API configuration via the Settings page.
# You can set your API key and base URL there at runtime.
#
# For server-side proxy (Netlify functions), set:
# POLLINATIONS_API=your_secret_key_here
